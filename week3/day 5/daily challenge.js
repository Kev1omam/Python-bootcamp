let sentence = "'This dinner is not that bad ! You cook well',"
let startIndex = sentence.search("not");
let EndIndex = sentence.search("bad");
let EndIndex = sentence.search("bad") + 3 ;
let array = sentence.split("");
startIndex = array.indexOf('bad');
EndIndexInd = array.indexOf("bad");
array.splice("startInd , (E)")
